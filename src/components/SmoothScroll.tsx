'use client';

import { useEffect } from 'react';

export default function SmoothScroll() {
  useEffect(() => {
    // Lenis smooth scroll is initialized in the main page component
    // This component is a placeholder for any additional smooth scroll logic
    return () => {
      // Cleanup if needed
    };
  }, []);

  return null;
}
